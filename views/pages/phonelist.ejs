<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/dist/output.css" />
    <title>HCS :: PhoneBook</title>
</head>
<body>

    <div class="p-4">
        <h1 class="pb-4 text-2xl font-bold">Staff Phonelist</h1>

        <table class="border-collapse border border-slate-500 w-1/3">
            <thead>
                <tr>
                    <th class="p-2 text-left border-collapse border border-slate-500">Name</th>
                    <th class="p-2 text-left border-collapse border border-slate-500">Extension</th>
                </tr>
            </thead>
            <tbody>
                <% data.forEach((d)=> { %>
                    <tr>
                        <td class="p-1 border-collapse border border-slate-500"><%= d.name %></td>
                        <td class="p-1 border-collapse border border-slate-500"><%= d.extension %></td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

</body>
</html>